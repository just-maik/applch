{
  "name": "applch",
  "version": "0.0.7-alpha",
  "description": "An AI application checker in alpha (dont use), Only works with bun right now",
  "type": "module",
  "bin": {
    "applch": "./dist/applch.js"
  },
  "main": "./dist/applch.js",
  "module": "./dist/applch.js",
  "files": [
    "dist"
  ],
  "keywords": [
    "cli",
    "ai",
    "application",
    "checker",
    "bun"
  ],
  "author": "Maik",
  "engines": {
    "bun": ">=1.3.0"
  },
  "private": false,
  "scripts": {
    "start": "bun run applch.ts",
    "build": "bun build applch.ts --outdir=dist --outfile=applch.js --target=bun",
    "prepublishOnly": "bun run build"
  },
  "dependencies": {},
  "devDependencies": {
    "@inkjs/ui": "^2.0.0",
    "@perplexity-ai/perplexity_ai": "^0.17.0",
    "citty": "^0.1.6",
    "ink": "^6.5.1",
    "marked": "^16.0.1",
    "marked-terminal": "^7.3.0",
    "@types/bun": "latest",
    "@types/marked-terminal": "^6.1.1"
  }
}